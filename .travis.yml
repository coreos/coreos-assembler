dist: xenial

services:
    - docker

language: python
python:
    - "3.6"

env:
    - SCVERSION="stable"

script:
    - docker build -t coreos-assembler-test -f Dockerfile .

notifications:
  email: false
